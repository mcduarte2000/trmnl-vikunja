# Local-only configuration for trmnl_preview.
# Plugin settings and templates remain under src/.
custom_fields:
  api_token: "tk_56d06b95c30a80e4d493b1a3eeadf10c3a73856e"
  # Updated to the public Vikunja endpoint without a trailing slash.
  # Removing the trailing slash prevents double‑slash URLs (e.g. `...net//api/...`).
  base_url: "https://vikunja.miguelduarte.net"
  project_ids: "3"
  view_mode: "kanban"
  status_filter: "all"
  cf_access_client_id: "{{7b9c7a8e484549ec72f65e2b575b8e2a.access}}"
  cf_access_client_secret: "{{cfast_NDUn2kwgG8Gw3EwZsGjt3t7gDTP1pSoRUw7VBCbRbf6952ae}}"
variables:
  trmnl:
    plugin_settings:
      # Use the base_url variable to build the polling URL, ensuring no double slash.
      # Use the project‑specific tasks endpoint to ensure JSON responses for Kanban view.
      polling_url: "{{ base_url }}/api/v1/projects/{{ project_ids }}/tasks"